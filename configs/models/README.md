# Model Staging

Place local ONNX or OCR model files here when provisioning native inference adapters.

- `ocr/` for OCR models
- `vision/` for panel or scene analysis models
- `tts/` for optional local narration models

The current codebase uses deterministic mock adapters until these runtimes are provisioned.
